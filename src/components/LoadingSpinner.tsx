import { FC } from "react";

const LoadingSpinner: FC = () => {
    return(
        <div className="animate-spin rounded-full h-5 w-5 border-y-2 border-white mx-2"></div>
    )

}

export default LoadingSpinner